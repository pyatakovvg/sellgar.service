
// import { bindToExchange } from "@sellgar/rabbit";

// import { galleryDestroy } from '../actions/gallery';


export default async function() {
  // const salt = Date.now().toString(32);

  // await bindToExchange(process.env['QUEUE_IMAGE_DELETE'] + '_' + salt, process.env['EXCHANGE_IMAGE_DELETE'], async (data, cb) => {
  //   const result = JSON.parse(data);
  //
  //   await galleryDestroy(result);
  //
  //   cb(true);
  // });
}
